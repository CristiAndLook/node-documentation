// A A A
describe('Test in the App file', () => {

    test('should be 30', () => {

        // 1. Arrange
        const value = 10;

        // 2. Act
        const result = value + 20;

        // 3. Assert
        expect(result).toBe(30);
        

    })
})